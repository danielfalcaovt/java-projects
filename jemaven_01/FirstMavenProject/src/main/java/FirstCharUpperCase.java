import java.util.Iterator;
import java.util.stream.IntStream;

public class FirstCharUpperCase {
    public static String toUpperCaseFirstChar(String word)  {
        IntStream characters = word.chars();
        return "";
    }
}
